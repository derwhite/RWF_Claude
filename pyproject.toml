[project]
name = "rtwf-tracker"
version = "0.1.0"
description = "Race to World First Pull-Tracker (Flask + waitress + raider.io API)"
requires-python = ">=3.10"
dependencies = [
    "flask==3.0.3",
    "waitress==3.0.0",
    "requests==2.32.3",
    "python-dotenv==1.0.1",
    "tzdata; sys_platform == 'win32'",
]

[tool.uv]
package = false
